name: orbit_cola
channels:
  - defaults
dependencies:
  - python=3.10
  - matplotlib
  - numpy
  - pandas
  - pip
  - scipy
  - requests
  - pip:
      - astropy
      - dash
      - dash_bootstrap_components
      - mysql-connector-python
      - sgp4
      - sshtunnel
